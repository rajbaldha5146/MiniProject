<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-lg py-2 px-3 text-sm mb-5 inline-block" href="/logout">LogOut</a>
        </div>
        <h3 class="text-3xl mb-1"><span class="font-light">Hello</span>, <%= user.name %>👋</h3>
        <h5 class="mb-5">You Can Create A New Post.</h5> 
        <form method="post" action="/post">
            <textarea placeholder="What's on your mind ?" class="block w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-lg" name="content"></textarea>
            <input class="px-3 py-2 w-40 bg-blue-500 block rounded-lg mt-2 text-sm" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
        </div>
        <div class="postcontainer flex flex-wrap gap-5 mt-5">
           <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 rounded-lg mb-4 bg-zinc-800 p-4 border-[1px] border-zinc-700">
                <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                <p class="text-sm tracking-tight"><%= post.content %></p>
                <small class="mt-2 inline-block"><%= post.likes.length %></small> Likes</small>
                <div class="btns flex gap-4">
                    <a class="text-blue-500 " href="/like/<%= post._id %>">
                        <%=post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                    </a>
                    <a class="text-zinc-500 " href="/edit/<%= post._id %>">Edit</a>
                </div>
            </div>
           <% }) %>
        </div>
    </div>
</body>
</html>