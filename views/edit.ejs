<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-lg py-2 px-3 text-sm mb-5 inline-block" href="/logout">LogOut</a>
        </div>
        <h5 class="mb-5">Edit Your Post.</h5> 
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind ?" class="block w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-lg" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 w-40 bg-yellow-500 text-black block rounded-lg mt-2 text-sm" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>